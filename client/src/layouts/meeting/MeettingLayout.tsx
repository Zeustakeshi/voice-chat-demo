import { Outlet } from "react-router-dom";

const MeettingLayout = () => {
    return (
        <div className="">
            <Outlet></Outlet>
        </div>
    );
};

export default MeettingLayout;
